import 'package:flutter/material.dart';

class TodoDetailPage extends StatelessWidget {
  const TodoDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('TodoDetailPage');
  }
}
